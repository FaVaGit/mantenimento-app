# TLS Certificates

Questa directory e il mount predefinito usato da `docker-compose.tls.yml`.

File attesi:
- `fullchain.pem`
- `privkey.pem`

Per test locale puoi montare certificati self-signed.
Per staging usa certificati emessi per il dominio o sottodominio dedicato.