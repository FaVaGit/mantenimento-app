# Node runtime
NODE_ENV=production
PORT=3000
HTTP_PORT=80
HTTPS_PORT=443
TLS_CERTS_DIR=./certs

# Minimal privacy-preserving access logging
ACCESS_LOG_ENABLED=true
ACCESS_LOG_SALT=replace-with-a-random-secret-value

# API protection
CALCULATE_RATE_WINDOW_MS=60000
CALCULATE_RATE_MAX_REQUESTS=30